name: dev-ci

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v1

      - name: Hello code!!
        run: echo "Hello code!! from ${{ github.repository}} triggered by the ${{github.event_name}} done by ${{github.actor}}"